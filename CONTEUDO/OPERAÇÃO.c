#include <stdio.h>

int man({

    float m;
    
    printf ("insira a nota:\n");
    scanf("%f", &m);

// no caso && seria (e) quando duas 
// situações forem verdadeiras ao mesmo 
// tempo

    if (m >= 7.0 && m => 10){
        printf ("aprovado (a) \n");
    }

// no caso || seria (ou) quando so serar 
// verdadeiro se a menos 1 for 

    if (m >= 7.0 || m => 10){
        printf ("aprovado (a) \n");
    }
// no caso ! seria (negação)

     if (m >= 7.0 ! m => 10){
        printf ("aprovado (a) \n");
    }
})